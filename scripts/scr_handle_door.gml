///scr_handle_door(door_x, door_y)
dx = argument[0];
dy = argument[1];

switch(dx) {
    case 128: //Door X here
        //If Multiple doors in the door x
        if (dy == 96) { //Replace 'doory' with the door y
            room_goto(rm_outside);
        } else if (dy = doory) { //Replace 'doory' with the door y
            //Code for door
        }
        
    break;
    
    case 1: //Door X here
        //If Multiple doors in the door x
        if (dy == doory) { //Replace 'doory' with the door y
            //Code for door
        } else if (dy = doory) { //Replace 'doory' with the door y
            //Code for door
        }
        
    break;
}
